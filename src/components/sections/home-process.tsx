'use client';

import { V2Process } from '@/components/sections/v2-process';

/**
 * Homepage process section — reuses the Journey sticky scroll from restaurants.
 * Thin wrapper to keep page assembly clean.
 */
export function HomeProcess() {
    return <V2Process />;
}
